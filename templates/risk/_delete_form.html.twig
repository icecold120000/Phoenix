<form method="post" action="{{ path('risk_delete', {'id': risk.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce risque ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ risk.id) }}">
    <button class="btn btn-danger"><i class="fas fa-trash"></i></button>
</form>
