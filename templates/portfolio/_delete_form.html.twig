<form method="post" action="{{ path('app_portfolio_delete', {'id': portfolio.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce portfolio ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ portfolio.id) }}">
    <button class="btn btn-danger"><i class="fas fa-trash"></i></button>
</form>
