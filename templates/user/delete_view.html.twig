{% extends 'base.html.twig' %}

{% block title %}Suppression d'un utilisateur {% endblock %}

{% block body %}
	<main>
	    <h1>Suppression d'un utilisateur</h1>

	    <h2>Appuyer sur le bouton Supprimer pour supprimer l'utilisateur choisi : {{user.firstnameUser}} {{user.lastnameUser}}</h2>

        {% for message in app.flashes('FailedDeleteUser') %}
            <div class="flash-danger" id="warning">
                <p>{{ message|replace({".":".</br>"})|raw }}</p>
            </div>
        {% endfor %}


	    {{ include('user/_delete_form.html.twig') }}
		<div>
		    <a href="{{ path('user_index') }}"
		     class="btn btn-secondary" id="btnRetourSuppr">
		        Revenir à la liste
		    </a>
	    </div>
	</main>
{% endblock %}