<form method="post" action="{{ path('app_budget_delete', {'id': budget.id, 'projectId':project.id }) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce budget');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ budget.id) }}">
    <button class="btn btn-danger"><i class="fas fa-trash"></i></button>
</form>
