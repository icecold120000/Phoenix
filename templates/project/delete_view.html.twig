{% extends 'base.html.twig' %}

{% block title %}Suppression d'un projet {% endblock %}

{% block body %}
    <main>
        <h1>Suppression d'un projet</h1>

        <h2>Appuyer sur le bouton Supprimer pour supprimer le projet choisi : {{ project.nameProject }}</h2>

        {% for message in app.flashes('FailedDeleteProject') %}
            <div class="flash-danger" id="warning">
                <p>{{ message|replace({".":".</br>"})|raw }}</p>
            </div>
        {% endfor %}

        {{ include('project/_delete_form.html.twig') }}
        <div>
            <a href="{{ path('app_homepage') }}"
               class="btn btn-secondary" id="btnRetourSuppr">
                Revenir à la liste
            </a>
        </div>
    </main>
{% endblock %}